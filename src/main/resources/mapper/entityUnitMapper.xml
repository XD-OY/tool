<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace必须和Mapper接口类路径一致 -->
<mapper namespace="com.chaos.tool.dao.entityUnitMapDao">
    <select id="createEntity" parameterType="String" resultType="String">
     SELECT MAX(B.COMMENTS) FROM DBA_TABLES A ,DBA_TAB_COMMENTS B  WHERE  A.TABLE_NAME=B.TABLE_NAME AND  A.TABLE_NAME= #{table}  AND  B.OWNER='HRPDEV'
    </select>
</mapper>